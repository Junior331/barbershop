import { Layout } from "@/components/templates";

export const Calendar = () => {
  return (
    <Layout>
      <div className="flex justify-center items-center h-screen w-full">
        <h1 className="text-3xl font-bold">Bem-vindo à Calendar!</h1>
      </div>
    </Layout>
  );
};
