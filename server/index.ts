// import express from "express";
// import cors from "cors";

// const app = express();
// const PORT = 3001;

// app.use(cors());
// app.use(express.json());

// app.get("/api/data", (req, res) => {
//   res.json({ message: "Dados do Gateway" });
// });

// app.listen(PORT, () => {
//   console.log(`Gateway rodando na porta ${PORT}`);
// });
